import 'package:flutter/material.dart';

// store the IconTheme colour in a class so that they can be accessed from anywhere
class IosColors {
  static const Color iosYellow = Color.fromRGBO(255, 213, 46, 1);
  static const Color iosGrey = Color.fromARGB(255, 48, 48, 48);
  static const Color iosLightGrey = Color.fromARGB(255, 156, 156, 156);
  static const Color iosWhite = Color.fromRGBO(229, 229, 229, 100);
}
